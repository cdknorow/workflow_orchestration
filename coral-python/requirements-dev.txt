# Development + macOS app build dependencies
# Install with: pip install -r requirements-dev.txt

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
aiosqlite>=0.19.0
httpx>=0.25.0
python-multipart>=0.0.6

# System tray (macOS menu bar)
rumps>=0.4.0

# macOS .app bundling
py2app>=0.28.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Linting / formatting
flake8>=6.0.0
black>=23.0.0

# Packaging / publishing
build>=1.0.0
twine>=5.0.0
